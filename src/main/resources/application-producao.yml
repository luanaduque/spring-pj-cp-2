spring:
  datasource:
    url: jdbc:oracle:${DB_PROD_SERVER:thin:@oracle.fiap.com.br}:${DB_PROD_PORT:1521}:${DB_PROD_NAME:ORCL}
    username: ${DB_PROD_USERNAME:rm550813}
    password: ${DB_PROD_PASSWORD:261104}
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    show-sql: true
